package org.layz.hx.config.persist.dao.schedule;

import org.layz.hx.config.entity.schedule.ScheduleCron;
import org.layz.hx.persist.repository.BaseDaoImpl;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ScheduleCronDaoImpl extends BaseDaoImpl<ScheduleCron> implements ScheduleCronDao {

    private static final Logger LOGGER = LoggerFactory.getLogger(ScheduleCronDaoImpl.class);

}