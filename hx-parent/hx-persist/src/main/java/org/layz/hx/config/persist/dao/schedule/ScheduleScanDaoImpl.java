package org.layz.hx.config.persist.dao.schedule;

import org.layz.hx.config.entity.schedule.ScheduleScan;
import org.layz.hx.persist.repository.BaseDaoImpl;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ScheduleScanDaoImpl extends BaseDaoImpl<ScheduleScan> implements ScheduleScanDao {

    private static final Logger LOGGER = LoggerFactory.getLogger(ScheduleScanDaoImpl.class);
}